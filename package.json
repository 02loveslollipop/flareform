{
  "name": "flareform",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "format": "prettier --write src scripts test github-action/src eslint.config.js package.json github-action/package.json wrangler.jsonc",
    "format:check": "prettier --check src scripts test github-action/src eslint.config.js package.json github-action/package.json wrangler.jsonc",
    "lint": "eslint src scripts test github-action/src eslint.config.js",
    "test:unit": "node --test test/unit/*.test.js",
    "test:integration": "node --test test/integration/*.test.js",
    "test:security": "node --test test/security/*.test.js",
    "test": "npm run test:unit && npm run test:integration && npm run test:security",
    "build": "node scripts/build.js",
    "migrate:local": "node scripts/migrate.js --local",
    "migrate:remote": "node scripts/migrate.js --remote",
    "policy:validate": "node scripts/sync-config.js --validate --config-dir config",
    "policy:dry-run": "node scripts/sync-config.js --dry-run --config-dir config",
    "verify:action-dist": "node scripts/verify-action-dist.js",
    "check": "npm run format:check && npm run lint && npm test && npm run build && npm run verify:action-dist"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "eslint": "10.10.0",
    "prettier": "3.9.6",
    "wrangler": "4.131.1"
  },
  "dependencies": {
    "jose": "6.2.12",
    "yaml": "2.9.1"
  }
}
